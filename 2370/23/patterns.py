

def pat1(dd: dict[int, int], ...) -> None:
    dd[...key...]


def pat2(dd: dict[str, str]) -> None
    for key in dd.keys():
        val = dd[key]


def pat3() -> dict[str, str]:
    # Start an empty one
    result = {}
    # Put everything in it
    result['flavor'] = 'green'
    # Return it when it's finished
    return result
